rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client-side reads and writes.
    // All application data access is handled securely server-side via Next.js Server Actions & Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
